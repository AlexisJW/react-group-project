import React from 'react';
import Navbar from '../components/Navbar';

const Profile = () => (
  <main className="profile-main">
    <Navbar />
    <section className="profile-section">Profile</section>
  </main>
);

export default Profile;
